---
"@tmelliott/react-rserve": minor
---

Enhanced `useWidget` hook with support for child widgets. The hook now properly handles widgets that contain other widgets as children, enabling hierarchical widget structures. The return type now includes a `children` property when child widgets are present.

